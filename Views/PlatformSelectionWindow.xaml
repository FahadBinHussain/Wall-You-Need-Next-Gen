<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Wall_You_Need_Next_Gen.Views.PlatformSelectionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wall_You_Need_Next_Gen.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Select Wallpaper Platform">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid x:Name="CustomTitleBar" Grid.Row="0" Height="32" VerticalAlignment="Top">
            <TextBlock Text="Select Wallpaper Platform" 
                       FontSize="16" 
                       FontWeight="SemiBold" 
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Margin="0,20,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater x:Name="PlatformsRepeater">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="180" MinItemHeight="180" MinRowSpacing="16" MinColumnSpacing="16"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Button Click="PlatformButton_Click" 
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Padding="12"
                                MinWidth="180"
                                MinHeight="180"
                                CornerRadius="8">
                            <StackPanel>
                                <FontIcon Glyph="&#xE8B7;" FontSize="32" Margin="0,0,0,12"/>
                                <TextBlock Text="{Binding}" 
                                           TextWrapping="Wrap"
                                           HorizontalAlignment="Center"
                                           FontSize="16"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Window>