<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Wall_You_Need_Next_Gen.Views.LatestWallpapersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wall_You_Need_Next_Gen.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Transitions for smooth animations when layout changes -->
        <TransitionCollection x:Key="LayoutTransitions">
            <RepositionThemeTransition />
            <AddDeleteThemeTransition />
            <ContentThemeTransition />
            <ReorderThemeTransition />
        </TransitionCollection>
        
        <!-- Animation for smooth resizing -->
        <Storyboard x:Key="ResizeAnimation">
            <DoubleAnimation Storyboard.TargetName="WallpapersGridView" 
                            Storyboard.TargetProperty="Opacity"
                            From="0.97" To="1" Duration="0:0:0.3" />
        </Storyboard>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Latest wallpapers" Style="{StaticResource TitleTextBlockStyle}" Margin="0,24,0,16" />
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="FilterButton" Style="{StaticResource DefaultButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Image Source="/Assets/settings.png" Width="16" Height="16" />
                        <TextBlock Text="Filter" />
                    </StackPanel>
                </Button>
                
                <Button x:Name="SlideshowButton" Style="{StaticResource DefaultButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Image Source="/Assets/slideshow.png" Width="16" Height="16" />
                        <TextBlock Text="Set as slideshow" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Responsive wallpapers grid -->
        <GridView x:Name="WallpapersGridView" 
                  Grid.Row="1" 
                  Margin="0,8,0,0"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemContainerTransitions="{StaticResource LayoutTransitions}"
                  SizeChanged="WallpapersGridView_SizeChanged">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid x:Name="WallpapersWrapGrid"
                                  Orientation="Horizontal"
                                  HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <local:WallpaperCard Margin="8" Width="300" Height="180" />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page> 